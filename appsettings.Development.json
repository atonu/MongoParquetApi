{
  "Mongo": {
    "ConnectionString": "mongodb://localhost:27017",
    "Database": "sampledb",
    "Collection": "items"
  },
  "Storage": {
    "Type": "Local", // "Local" or "AzureBlob"
    "LocalFolder": "storage/parquet", // used if Type=Local
    "Azure": {
      "ConnectionString": "", // used if Type=AzureBlob
      "Container": "parquetfiles"
    }
  },
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  }
}
